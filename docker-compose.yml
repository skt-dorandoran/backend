name: drdr

services:
  app:
    build: .
    image: drdr-backend
    container_name: drdr-backend
    ports:
      - "8000:8000"
    networks:
      - dorandoran

networks:
  dorandoran:
    name: dorandoran
